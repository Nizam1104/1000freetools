import { Metadata } from "next";

interface ColorToolsLayoutProps {
  children: React.ReactNode;
}

export default function ColorToolsLayout({ children }: ColorToolsLayoutProps) {
  return <>{children}</>;
}
